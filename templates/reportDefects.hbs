<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=chrome">
    <title>Reporte de Defectos</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://d3js.org/d3.v5.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/apexcharts"></script>
</head>

<body>
    <div class="container">
        <h5 style="text-align:center;">Reporte de defectos</h5>
        <br>
        <div style="
        margin-bottom: 2rem;
        display: flex; 
        flex-direction: row; 
        flex-wrap: wrap; 
        justify-content:space-between; 
        align-items: center;">
            <div>Proyecto: {{project.title}}</div>
            <div>&nbsp;Fecha de generación: {{reportDate}}</div>
        </div>
        <h5 style="margin-bottom: 1rem;" id="defects">Defectos reportados</h5>
        <table class="table">
            <thead class="table-primary">
                <th>Etiqueta</th>
                <th>Nombre</th>
                <th>Severidad</th>
                <th>Prioridad</th>
                <th>Estado</th>
            </thead>
            <tbody>
                {{#each defects}}
                <tr>
                    <td>{{tag}}</td>
                    <td>{{title}}</td>
                    <td>{{severity.name}}</td>
                    <td>{{priority.name}}</td>
                    <td>{{defectState.name}}</td>
                </tr>
                {{/each}}

            </tbody>
        </table>
    </div>
    <!--.container-->
</body>
<style>
    table,
    th,
    td {
        border: 1px solid black;
        border-collapse: collapse;
    }

    a {
        color: inherit;
        /* blue colors for links too */
        text-decoration: inherit;
        /* no underline */
    }
</style>

</html>